//arrays-collection of similar data type stored at contiguous(continuous) memory locations.
#include <stdio.h>

int main() {

    int marks[3] = {97, 98, 99}; //int type array
    //creates a memory location with name marks with 3 reserved blocks of 4 bytes each where indexing starts from 0 called 0-based indexing 

    char name [2] = {'Ram', 'Maya'};  //char type array
    float price[2]= {22.4, 35.5};  //float type array
    return 0;
}
//input and output
// scanf("%d",&marks[0]);
// printf("%d",marks[0]);

  